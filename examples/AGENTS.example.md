# Example allowlist note

Use this with the **RexBot / Rex Hub community pack for OpenClaw**.

Use profiles as starting points, then trim them per agent.

Example:
- default assistant: `generalist_builder`
- repo agent: `builder_engineering`
- docs agent: `docs_support`
- data agent: `data_analytics`
- GTM agent: `attention_builder` or `commercial_operator`
